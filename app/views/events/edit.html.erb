<% content_for :title, "編集する | ちょいあつ" %>
<h1 class="text-3xl font-bold text-center pt-10 pb-5"><%= t('application.edit') %></h1>

<%= render 'form' %>

<div class="text-center mt-6">
  <%= link_to t('edit.back_to_show'),
              event_path(@event, return_to: safe_return_to_path),
              class: "rounded-md px-4 py-2 bg-gray-200 hover:bg-gray-300 text-center font-medium" %>
</div>
